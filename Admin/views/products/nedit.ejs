<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Edit Product <%= product.id %></h1>
    <form action="/products/edit/<%= product.id %>" method="POST">
        <label for="productName">Product Name:</label>
        <input type="text" id="productName" name="productName" value="<%= product.productName %>" required>
        <label for="price">Price:</label>
        <input type="text" id="price" name="price" value="<%= product.price %>" required>
        <label for="oldPrice">Old Price:</label>
        <input type="text" id="oldPrice" name="oldPrice" value="<%= product.oldPrice %>">
        <label for="weight">Weight (comma separated):</label>
        <input type="text" id="weight" name="weight" value="<%= product.weight.join(',') %>">
        <label for="catImg">Category Image URL:</label>
        <input type="text" id="catImg" name="catImg" value="<%= product.catImg %>">
        <label for="discount">Discount:</label>
        <input type="text" id="discount" name="discount" value="<%= product.discount %>">
        <label for="brand">Brand:</label>
        <input type="text" id="brand" name="brand" value="<%= product.brand %>">
        <label for="productImages">Product Images (comma separated):</label>
        <input type="text" id="productImages" name="productImages" value="<%= product.productImages.join(',') %>">
        <label for="rating">Rating:</label>
        <input type="number" id="rating" name="rating" value="<%= product.rating %>" step="0.1" min="0" max="5">
        <label for="description">Description:</label>
        <textarea id="description" name="description"><%= product.description %></textarea>
        <button type="submit">Update Product</button>
    </form>
</body>
</html>
